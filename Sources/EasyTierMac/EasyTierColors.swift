import SwiftUI

enum EasyTierColors {
    static let statusConnected = Color("StatusConnected", bundle: .main)
    static let statusConnecting = Color("StatusConnecting", bundle: .main)
    static let statusError = Color("StatusError", bundle: .main)
    static let metricUpload = Color("MetricUpload", bundle: .main)
    static let metricDownload = Color("MetricDownload", bundle: .main)
    static let menuBarSelectedRow = Color("MenuBarSelectedRow", bundle: .main)
    static let menuBarConnected = Color("MenuBarConnected", bundle: .main)
}
